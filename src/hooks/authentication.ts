// import { HookContext } from '../declarations'
// import { logError } from './log-error'

// //thgolcynicyuxvlnkqkp.supabase.co/rest/v1/users?

// export async function authenticateUser(context: HookContext) {
//   const uid = context.arguments[0]
//   if (!process.env.SUPABASE_PROJECT_ID) {
//     logError(context, )
//   }
//   const supaUrl = `https://${process.env.SUPABASE_PROJECT_ID}`
//   const isExistingUser = await fetch()
// }
